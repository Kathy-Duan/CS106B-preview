#include <iostream>

using namespace std;

int main() {
    int number;
    int max = ;
    cin >> number;
    while (number != 0) {
        if (number > max) {
            max = number;
        }
    } 
}